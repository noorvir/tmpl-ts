/** @type {import('tailwindcss').Config} */
module.exports = {
  // Root config - enables Tailwind IntelliSense LSP to start
  // Each app has its own tailwind.config for specific settings
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};
